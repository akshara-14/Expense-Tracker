import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';
// Here, App is a component which is used like a custom HTML element
ReactDOM.render(<App />, document.getElementById('root'));
